@model Mantex.ERP.Entities.MachineStatusEnum
@{
    var panelClass = Model < Mantex.ERP.Entities.MachineStatusEnum.Working
        ? "text-warning" : "text-success";
}
<div class="panel-body text-center @panelClass">
    @ModelAsText()
</div>

@functions{
    string ModelAsText()
    {
        switch (Model)
        {
            case Mantex.ERP.Entities.MachineStatusEnum.Offline: return "FlowScanner är offline.";
            case Mantex.ERP.Entities.MachineStatusEnum.Online: return "FlowScanner är online men inte startad.";
            case Mantex.ERP.Entities.MachineStatusEnum.Working: return "FlowScanner väntar på transaktion.";
            case Mantex.ERP.Entities.MachineStatusEnum.WaitingForEmptyBelt: return "Vänta på klartecken för lastning.";
            case Mantex.ERP.Entities.MachineStatusEnum.Measuring: return "Klart att lasta material på bandet.";
            default:
                throw new NotImplementedException();
        }
    }
}